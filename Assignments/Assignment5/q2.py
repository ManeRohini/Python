#predict o/p

b=bytes([65,66,67])
print(b)

#here list of int data type is passed to the bytes, range is between 0 to 255
#byte is immutable data type
#here every int gets stored in binary format and gets printed its corresponding ASCII value