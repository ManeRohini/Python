#Explain
#id() function returns the unique identity that is memory address
#this helps to undersatnd how python manages memory and reference to object\
#Mutable objects (like lists, dicts)  allocate memoey seperately, 
#while immutable ones (like tuples, ints) may share memory.

